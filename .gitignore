# Local-only: 2MB self-contained build used just for Claude Artifact previews, not needed for the live site
preview-artifact.html

# Local-only: raw photo uploads staged before processing into img/
image import/
